// Step 1: Check if you already have a key. Type "ls ~/.ssh". If you see "id_rsa" and "id_rsa.pub", you can skip to Step 3.

// Step 2: If you don't have one, generate a new pair. Type "ssh-keygen -t rsa -b 4096".
// Step 2.1: It will ask where to save it. Just press "Enter" to use the default location (/home/user/.ssh/id_rsa).
// Step 2.2: It will ask for a passphrase. This is an extra layer of security. You can leave it blank by pressing "Enter" twice, or add one for better security.

// Step 3: Now we need to find your PUBLIC key. This is the one ending in ".pub".

// Step 4: Use "cat ~/.ssh/id_rsa.pub" to display the content of your public key. It should start with "ssh-rsa" followed by a long string of random characters.

// Step 5: Copy that entire string carefully.

// Step 6: Create the exercise folder: "mkdir ex03 && cd ex03".

// Step 7: Create the file for turn-in by pasting your key into it: "echo 'YOUR_KEY_STRING_HERE' > id_rsa_pub".

// Note: Make sure the filename is exactly "id_rsa_pub" as requested in the subject.
// Step 8: Now for the "Intranet" part. You need to go to your 42 Profile settings (usually under your profile picture -> Settings -> SSH Keys).

// Step 9: Click "New SSH Key", give it a title (like "Cluster iMac"), and paste that same public key string there.

// Step 10: Save it. This "handshake" allows the 42 Git server to recognize your computer when you try to "git push" your work.